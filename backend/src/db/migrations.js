/**
 * Database Migrations
 * Simple migration runner - database is initialized in app.ts on startup
 */

console.log('✅ Migrations completed (database initialized on server startup)')
process.exit(0)
