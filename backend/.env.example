# Server Config
NODE_ENV=development
PORT=8080
LOG_LEVEL=info

# External APIs (add your own keys)
FLASH_ALPHA_API_KEY=
THETA_DATA_API_KEY=

# Database (optional - for production backtests)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=trader_app
DB_USER=postgres
DB_PASSWORD=

# Optional: Sentry for error tracking
SENTRY_DSN=

# Optional: CORS settings
CORS_ORIGIN=http://localhost:3000
